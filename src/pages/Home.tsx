import Navigation from "../components/Navigation";

const Home = () => {
    return (
        <div className="game-container">
            <Navigation />
        </div>
    );
};

export default Home;